# aqavia_task-3

This project is built using **React + Vite** and uses **Redux Toolkit** for global state management.  
The app contains two pages, and both pages can update the same value.  
Any change made on one page is instantly visible on the other page.  
The value also stays saved even after refreshing the page.

---

## 📌 Features

- Update data from **Page 1**
- Update data from **Page 2**
- Both pages show the same value in real time
- Data stays saved using LocalStorage
- Simple and clean CSS design
- Ready for deployment on GitHub Pages

---

## 🚀 Run the project locally

After downloading/cloning the project:

```bash
npm install
npm run dev
